name: "Reusable Build & Deploy"

on:
  workflow_call:
    inputs:
      app_name:
        required: true
        type: string
      environment:
        required: true
        type: string
        description: "e.g., dv, prod"
      values_file:
        required: true
        type: string
        description: "Path to values file in app repo, e.g., charts/values-dv.yaml"
      # OPTIONAL: Add this so we can copy the secret if it exists
      secret_file:
        required: false
        type: string
        description: "Path to sealed secret file in app repo, e.g., charts/secret-dv.yaml"
      # REMOVED: helm_chart_name and helm_chart_version (No longer used/needed)

jobs:
  build-and-push:
    runs-on: tatahostit-runners
    outputs:
      image_tag: ${{ steps.calc_tag.outputs.tag }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Calculate Version Tag
        id: calc_tag
        run: |
          # Result: 2026.01.09.1645-a1b2c3d
          TAG="$(date +'%Y.%m.%d.%H%M')-$(git rev-parse --short HEAD)"
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "Generated Version: $TAG"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker

      - name: Docker Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: harbor.tatahostit.com/library/${{ inputs.app_name }}
          tags: |
            type=ref,event=branch
            type=raw,value=${{ steps.calc_tag.outputs.tag }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=harbor.tatahostit.com/library/${{ inputs.app_name }}:${{ github.ref_name }}
          cache-to: type=inline

  update-manifests:
    needs: build-and-push
    runs-on: tatahostit-runners
    steps:
      - name: Checkout App Code
        uses: actions/checkout@v4
        with:
          path: app-code

      - name: Install yq
        run: |
          sudo curl -L https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -o /usr/bin/yq
          sudo chmod +x /usr/bin/yq

      - name: Checkout Manifest Repo
        run: |
          git clone https://oauth2:${GLOBAL_MANIFEST_TOKEN}@github.com/TataHostIT/tatahostit-manifests.git manifest-repo
          cd manifest-repo
          git checkout main

      - name: Update Manifests
        env:
          APP: ${{ inputs.app_name }}
          ENV: ${{ inputs.environment }}
          TAG: ${{ needs.build-and-push.outputs.image_tag }}
        run: |
          # 1. Define Target Directory
          TARGET_DIR="manifest-repo/apps/${APP}"
          mkdir -p "$TARGET_DIR"

          # 2. Copy Config (Values)
          # Result: apps/user-service/dv-values.yaml
          TARGET_VALUES="$TARGET_DIR/${ENV}-values.yaml"
          cp "app-code/${{ inputs.values_file }}" "$TARGET_VALUES"

          # 3. Patch Image Tag in the new values file
          yq -i ".image.tag = \"${TAG}\"" "$TARGET_VALUES"
          
          # 4. Copy Secret (If input provided)
          if [ -n "${{ inputs.secret_file }}" ]; then
             if [ -f "app-code/${{ inputs.secret_file }}" ]; then
                echo "Copying Secret file..."
                # Result: apps/user-service/dv-secret.yaml (Matching our AppSet logic)
                cp "app-code/${{ inputs.secret_file }}" "$TARGET_DIR/${ENV}-secret.yaml"
             else
                echo "Warning: Secret file input was provided but file not found in repo."
             fi
          fi

      - name: Commit and Push
        working-directory: manifest-repo
        run: |
          git config user.name "CI Pipeline"
          git config user.email "ci@tatahostit.com"
          
          git add .
          
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "deploy(${{ inputs.app_name }}): update ${{ inputs.environment }} to $TAG"
            
            # PRO TIP: Pull rebase handles high-traffic conflicts better
            git pull --rebase origin main
            git push origin main
          fi